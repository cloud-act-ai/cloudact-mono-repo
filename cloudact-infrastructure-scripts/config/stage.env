# Stage Environment Configuration

# GCP Project
GCP_PROJECT_ID=gac-stage-471220
GCP_REGION=us-central1

# Cloud Run
SERVICE_NAME=convergence-pipeline-stage
CLOUD_RUN_REGION=us-central1
ALLOW_UNAUTHENTICATED=true

# Service Account
SERVICE_ACCOUNT=convergence-sa-stage@gac-stage-471220.iam.gserviceaccount.com

# KMS
KMS_KEYRING=convergence-keyring-stage
KMS_KEY=api-key-encryption
KMS_LOCATION=us-central1

# BigQuery
BIGQUERY_LOCATION=US

# Application
ENVIRONMENT=stage
LOG_LEVEL=DEBUG

# Resources
MEMORY=2Gi
CPU=2
TIMEOUT=300
MAX_INSTANCES=10
MIN_INSTANCES=0

# Image
IMAGE_NAME=gcr.io/gac-stage-471220/convergence-pipeline-stage
